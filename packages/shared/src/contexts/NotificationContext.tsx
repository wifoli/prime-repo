// Re-export Toast as NotificationContext for consistency
// Both patterns work the same way
export { ToastProvider as NotificationProvider, useToast as useNotification } from '../components/Toast.tsx';
